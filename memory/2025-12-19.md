# Memory Log - 2025-12-19

## Context Artifacts
- Added `docs/META_RD_NAVIGATOR.md` generated via `prompt-kits/drag-ready/drag-ready/coding/02-meta-rd-navigator.md` so every agent can reuse the concept map.
- `docs/PROJECT_CONTEXT.md` remains the canonical background doc; revisit whenever scope or dependencies change.

## Guidance for Future Agents
- For any new requirement, capture navigator + planning prompt outputs under `docs/` and append a new entry in `memory/` describing the delta.
- Keep the memory log chronological (one file per date) and reference relevant docs/prompts so context stays synchronized.
